# 双网关 + API-First + Auto-Registration 架构规范

> 本文档定义了通用的架构规范，适用于所有需要同时服务人类用户和 AI Agent 的系统。
> 其他项目可对照此规范检查自身架构合规性。

---

## 一、核心架构：双网关模型

```
┌──────────────────────────────────────────────────────┐
│                   双网关架构                            │
│                                                      │
│  ┌──────────────────┐      ┌──────────────────┐     │
│  │  API Server       │      │  Agent Gateway    │     │
│  │  (业务能力网关)    │◄────►│  (AI 能力网关)     │     │
│  │                   │      │                   │     │
│  │ • 数据 CRUD       │      │ • LLM 调用调度     │     │
│  │ • 外部 API 代理   │      │ • 多模态处理       │     │
│  │ • 业务逻辑        │      │ • 内容生成/分析    │     │
│  │ • 认证/鉴权       │      │ • Tool 编排        │     │
│  │ • 双模响应        │      │ • MCP 协议适配     │     │
│  └────────┬─────────┘      └─────────┬────────┘     │
│           │                          │               │
└───────────┼──────────────────────────┼───────────────┘
            │                          │
  ┌─────────┼──────────────────────────┼─────────┐
  │         ▼                          ▼         │
  │    ┌─────────┐              ┌──────────┐     │
  │    │ Web App │              │MCP Client│     │
  │    │ (展示层) │              │(任意Agent)│     │
  │    └─────────┘              └──────────┘     │
  └──────────────────────────────────────────────┘
```

### 职责划分

| 网关 | 职责 | 禁止 |
|------|------|------|
| **API Server** | 所有业务数据的 CRUD、外部 API 代理、认证鉴权、业务逻辑 | 禁止直接调用 LLM |
| **Agent Gateway** | 所有 AI 能力的调度（LLM 对话/生成/分析/识别）、Tool 编排、MCP 协议 | 禁止直接操作数据库 |

### 关键约束

- LLM 只有 Agent Gateway 可以直接调用，其他服务/模块禁止散装调用
- 数据库只有 API Server（及其下游业务服务）可以访问
- Agent Gateway 需要数据时，必须通过 API Server 获取
- API Server 需要 AI 能力时，必须通过 Agent Gateway 请求

---

## 二、API-First 开发流程

### 规范

任何新功能的开发必须遵循以下顺序：

```
1. 设计 API 接口（入参、出参、认证）
2. 实现 API（业务逻辑 + 数据层）
3. 添加 @AgentTool 装饰器（自动注册为 Agent Tool）
4. 添加双模响应（Agent 增强语义信息）
5. Web 前端消费 API 展示
```

### 检查清单

- [ ] 新功能是否先有 API 端点？
- [ ] Web 是否只通过 API 获取数据（无直接数据库查询）？
- [ ] API 是否标注了 `@AgentTool` 装饰器？
- [ ] 是否实现了双模响应（Agent 可获取语义增强信息）？
- [ ] Web 的 AI 功能是否通过 Agent Gateway 实现（而非自行调 LLM）？

---

## 三、Auto-Registration 机制

### @AgentTool 装饰器规范

每个对外暴露的 API 方法，如果希望 Agent 可调用，必须添加 `@AgentTool` 装饰器：

```typescript
@AgentTool({
  // 必填：工具唯一标识
  name: 'list-spaces',
  
  // 必填：自然语言描述（LLM 用来判断何时调用）
  description: '列出当前租户的所有知识空间，支持按关键词搜索和分页',
  
  // 必填：参数 Schema（用 zod 定义，自动转 JSON Schema）
  parameters: z.object({
    keyword: z.string().optional().describe('搜索关键词，模糊匹配空间名称'),
    page: z.number().optional().describe('页码，从 1 开始'),
    pageSize: z.number().optional().describe('每页数量，默认 20'),
  }),
  
  // 可选：Agent 专用提示
  agentHints: {
    whenToUse: '当用户想了解有哪些知识空间、想找某个空间时使用',
    outputFormat: '返回空间列表，每项包含空间名、文档数、最后更新时间',
    prerequisites: '无',
    followUp: '获取空间列表后，可以进一步查看某个空间的详细指标',
  },
})
```

### 自动注册流程

```
应用启动 → 扫描 @AgentTool 装饰器 → 生成 Tool Registry (JSON Schema)
                                              │
                          ┌───────────────────┼────────────────────┐
                          ▼                   ▼                    ▼
                   MCP Server 拉取       Agent Gateway 拉取    可选：自动生成文档
                   (暴露给外部客户端)    (内部 Tool 调用)      (Skill.md / API Doc)
```

### 检查清单

- [ ] 所有对外 API 是否都标注了 `@AgentTool`？
- [ ] Tool description 是否足够清晰（LLM 能理解何时该调用）？
- [ ] parameters 的每个字段是否都有 `.describe()` 说明？
- [ ] 是否有 agentHints 帮助 Agent 更好地使用该工具？

---

## 四、双模响应规范

### 触发方式

通过 `Accept` 请求头区分：
- `Accept: application/json` → 标准响应（Web 使用）
- `Accept: application/ai+json` → 增强响应（Agent 使用）

### 响应格式

**标准响应**（Web 消费）：
```json
{
  "data": { ... },
  "pagination": { "total": 42, "page": 1, "pageSize": 20 }
}
```

**增强响应**（Agent 消费）：
```json
{
  "data": { ... },
  "pagination": { "total": 42, "page": 1, "pageSize": 20 },
  "context": {
    "summary": "当前租户共有 42 个知识空间，其中 5 个近 7 天有更新",
    "highlights": ["空间 A 有 3 个待处理的诊断问题"],
    "suggestions": ["可以查看最近更新的空间", "可以筛选特定类型的空间"],
    "related_actions": ["list-space-metrics", "subscribe-space"]
  }
}
```

### context 字段规范

| 字段 | 必填 | 说明 |
|------|------|------|
| `summary` | 是 | 一句话自然语言摘要，概括返回数据的含义 |
| `highlights` | 否 | 需要 Agent 特别关注的信息点 |
| `suggestions` | 否 | 建议 Agent 接下来可以执行的操作 |
| `related_actions` | 否 | 相关的 Tool 名称列表 |

### 检查清单

- [ ] 是否实现了 DualResponse Interceptor？
- [ ] 每个 API 是否定义了 `context.summary` 的生成逻辑？
- [ ] Agent 增强响应是否比标准响应包含更多有用信息？

---

## 五、MCP 协议接入规范

### 双模传输

| 场景 | 传输方式 | 入口 |
|------|---------|------|
| 本地 IDE（Trae/Cursor/Claude Desktop） | stdio | `npx @your-org/mcp-cli` |
| 远程/SaaS/多租户 | SSE over HTTP | `Agent Gateway /mcp/sse` |

### MCP CLI 薄壳规范

MCP CLI 是一个轻量级进程，职责仅为协议转换：

```
MCP Client ←stdio→ MCP CLI ←HTTP→ Agent Gateway
```

- MCP CLI **不包含**业务逻辑
- MCP CLI **不直接**调用 LLM
- MCP CLI 通过环境变量获取认证信息
- MCP CLI 启动时从 Agent Gateway 拉取 Tool 列表

### 用户接入配置

```json
{
  "mcpServers": {
    "your-system": {
      "command": "npx",
      "args": ["@your-org/mcp-cli"],
      "env": {
        "API_ENDPOINT": "https://your-agent-gateway.com",
        "ACCESS_TOKEN": "<用户令牌>"
      }
    }
  }
}
```

### 检查清单

- [ ] Agent Gateway 是否暴露了 Tool Registry 端点？
- [ ] MCP CLI 是否为纯协议转换层（无业务逻辑）？
- [ ] 是否支持 stdio 和 SSE 双模传输？
- [ ] 第三方 Agent 能否通过 MCP 直接使用你的 Tools？

---

## 六、Agent Gateway 作为 AI 网关的规范

### AI 能力统一入口

系统中任何需要 LLM 的操作，必须通过 Agent Gateway：

```typescript
// ✅ 正确：通过 Agent Gateway
POST /api/agent/invoke { "skill": "diagnose-document", "params": {...} }
POST /api/agent/completion { "task": "summarize", "input": "..." }

// ❌ 错误：在业务服务中直接调用 LLM
import { OpenAI } from 'openai';
const result = await openai.chat.completions.create({...}); // 禁止！
```

### Agent Gateway 对内接口

```typescript
// 技能调用（编排式）
POST /api/agent/invoke
{
  "skill": string,         // 技能标识
  "params": object,        // 技能参数
  "stream": boolean,       // 是否流式
  "context"?: object       // 对话上下文
}

// 简单补全（原子式）
POST /api/agent/completion
{
  "task": "summarize" | "analyze" | "generate" | "translate" | "recognize",
  "input": string | { text?: string, imageUrl?: string },
  "options"?: { model?: string, temperature?: number }
}
```

### 检查清单

- [ ] 项目中是否存在绕过 Agent Gateway 的 LLM 直接调用？
- [ ] 业务 Worker 的 AI 逻辑是否已迁移为 Agent Skill？
- [ ] Web 的 AI 功能按钮是否通过 Agent Gateway 实现？
- [ ] 定时任务的 AI 操作是否通过 Agent Gateway 调度？

---

## 七、Skill 编排规范

### Tool 与 Skill 的关系

| 概念 | 粒度 | 来源 | 作用 |
|------|------|------|------|
| **MCP Tool** | 原子动作 | API 端点通过 `@AgentTool` 自动注册 | 让 Agent 调用单个业务能力 |
| **Skill** | 工作流编排 | 人工设计、审核和发布 | 指导 Agent 组合多个 Tools 完成业务任务 |

关键约束：

- MCP Tool 是 Agent 可调用的最小业务动作
- Skill 是 Agent 可执行的业务工作流，通常编排多个 Tools
- API 自动注册 MCP Tool 不应机械地一比一生成并发布 Skill
- 可以根据 Tool Registry 自动生成 Skill Draft，但 Skill Release 必须经过人工审核

### Skill.md 格式

```markdown
# 技能名称

## 描述
一句话描述该技能的作用

## 触发条件
描述 Agent 何时应该调用该技能

## 输入参数
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|

## 步骤编排
1. 调用 `tool-name-1` 做什么
2. 根据步骤 1 的结果，调用 `tool-name-2`
3. ...

## 输出格式
描述返回给用户的信息格式

## 异常处理
- 如果步骤 X 失败：...
```

### Agent Skills bundle 结构

Skill 发布单元必须遵循目录化 bundle 结构，避免自创仅 YAML 的不可扩展协议：

```text
skill-name/
  SKILL.md
  scripts/         # 可选：仅在 Agent Runtime sandbox 中执行
  references/      # 可选：长文档、协议、示例、知识材料
  assets/          # 可选：图片、模板、静态资源
  agents/
    openai.yaml    # 可选：特定 Agent 运行时配置
```

`SKILL.md` 使用 YAML frontmatter 描述元数据，正文使用 Markdown 指令：

```markdown
---
name: diagnose-document
description: Diagnose document quality problems and suggest fixes.
---

# Diagnose Document

## When to use
...
```

### 发布前 Tool-Skill 覆盖校验

每个新增或变更的 MCP Tool，都必须显式声明 Skill 覆盖策略：

```typescript
@AgentTool({
  name: 'list-spaces',
  description: '列出当前租户的所有知识空间',
  parameters: ListSpacesSchema,
  skillCoverage: {
    required: true,
    coveredBy: ['inspect-knowledge-space', 'prepare-tenant-report'],
  },
})
```

如果 Tool 不需要 Skill 覆盖，必须给出原因：

```typescript
@AgentTool({
  name: 'health-check',
  description: '检查服务健康状态',
  parameters: z.object({}),
  skillCoverage: {
    required: false,
    reason: '仅用于运维探活，不属于用户可执行业务工作流',
  },
})
```

CI / 发布流程必须检查：

- 新增 MCP Tool 是否声明了 `skillCoverage`
- `required: true` 的 Tool 是否至少被一个 Skill Release 覆盖
- `coveredBy` 引用的 Skill 是否存在且处于 `published` 状态
- `required: false` 是否包含可审计的 `reason`
- Tool 参数变更是否触发相关 Skill 重新审核

### 动态 Skill 发布模型

需要动态发布 Skill 的系统，必须将 Skill 作为版本化资源管理：

```text
draft -> review -> published -> superseded
                            \-> disabled
```

| 状态 | Agent 可发现 | 说明 |
|------|-------------|------|
| `draft` | 否 | 草稿，可由 Tool Registry 自动生成或人工创建 |
| `review` | 否 | 待审核版本，允许预览和测试 |
| `published` | 是 | Agent Runtime 可发现、可执行的正式版本 |
| `superseded` | 否 | 被新版本替代，保留历史和回滚能力 |
| `disabled` | 否 | 已下线版本，保留审计记录 |

发布约束：

- Agent 只能发现 `published` Skill
- 同一个 Skill name 同时最多一个 `published` release
- 发布新版本时，旧的 `published` release 自动进入 `superseded`
- `disabled` 表示从 Agent discovery 中下线，但不得删除历史记录
- Skill Release 必须记录版本、发布人、发布时间、bundle hash、审核记录

### Skill 权威来源

系统必须区分 Built-in Skill 和 Skill Release：

| 类型 | 来源 | 用途 |
|------|------|------|
| **Built-in Skill** | 随代码仓库或部署镜像提供 | seed、fallback、基础能力 |
| **Skill Release** | API Server 或业务控制面管理 | Agent Runtime 实际发现和执行的动态版本 |

对需要动态发布的系统：

- API Server 或业务控制面是 Skill Release 的权威来源
- Agent Gateway 不应把本地文件系统作为动态 Skill 的权威来源
- Agent Gateway 可以缓存 Skill bundle，但必须以 Skill Release manifest 的 hash / version 为准
- Built-in Skill 可用于初始化默认 Skill Release，但初始化后运行时发现应以发布记录为准

### 运行时同步

Agent Gateway 在启动 Agent task 前必须同步 Skill manifest：

```text
Agent task starts
  -> Agent Gateway requests published Skill manifest from API Server
  -> compare local lock/hash
  -> download changed Skill bundles
  -> verify bundle hash and signature
  -> materialize to Agent Runtime Skill directory
  -> start Agent Runtime
```

同步约束：

- manifest 必须只包含 `published` Skill
- bundle 下载后必须校验 hash，必要时校验签名
- 同步失败时，可按策略使用最近一次已验证缓存，但必须记录告警
- Agent task 运行期间使用启动时锁定的 Skill 版本，不应被中途热更新替换
- Agent Gateway 只负责同步、缓存和物化，不负责审核 Skill 内容

### scripts 执行边界

Skill bundle 可以包含 `scripts/`，但必须严格限制执行边界：

- API Server 可以存储、校验、版本化、分发 Skill bundle
- API Server 禁止执行 Skill scripts
- Agent Gateway 禁止在自身服务进程中执行 Skill scripts
- Skill scripts 只能在 Agent Runtime 的 workspace / sandbox 中执行
- 私有化部署可以允许管理员或运维发布带 scripts 的 Skill，但必须保留发布审计、bundle hash 和运行日志
- scripts 执行必须继承 Agent Runtime 的权限边界，不能获得 API Server 数据库直连权限

### 检查清单

- [ ] 复杂业务流程是否定义了 Skill？
- [ ] Skill 是否仅使用已注册的 Tools？
- [ ] Skill 描述是否清晰到 LLM 能理解执行？
- [ ] 新增 MCP Tool 是否声明了 Skill 覆盖策略？
- [ ] 需要 Skill 覆盖的 Tool 是否被至少一个 `published` Skill 覆盖？
- [ ] Skill Release 是否遵循 `draft -> review -> published -> superseded | disabled` 生命周期？
- [ ] Agent Gateway 是否在启动 Agent task 前同步 published Skill manifest？
- [ ] Skill scripts 是否只在 Agent Runtime workspace / sandbox 中执行？

---

## 八、动态 Skill 发布系统规范

### Skill Release API

动态 Skill 发布系统应由 API Server 或业务控制面提供管理 API：

```typescript
// 创建草稿
POST /api/skills/releases
{
  "name": string,
  "version": string,
  "bundle": SkillBundle,
  "source": "manual" | "generated-from-tool-registry"
}

// 提交审核
POST /api/skills/releases/{releaseId}/submit-review

// 发布版本
POST /api/skills/releases/{releaseId}/publish

// 下线版本
POST /api/skills/releases/{releaseId}/disable

// Agent Gateway 拉取 published manifest
GET /api/skills/manifest?status=published
```

### Skill Manifest 格式

```json
{
  "generatedAt": "2026-06-03T00:00:00Z",
  "skills": [
    {
      "name": "diagnose-document",
      "version": "1.2.0",
      "releaseId": "skillrel_123",
      "status": "published",
      "description": "Diagnose document quality problems and suggest fixes.",
      "bundleUrl": "https://api.example.com/api/skills/releases/skillrel_123/bundle",
      "bundleHash": "sha256:...",
      "coveredTools": ["get-document", "list-document-issues", "create-fix-plan"]
    }
  ]
}
```

### 运行边界

| 组件 | 可以做 | 禁止 |
|------|--------|------|
| **API Server / 控制面** | 存储、校验、审核、版本化、分发 Skill bundle | 执行 Skill scripts |
| **Agent Gateway** | 拉取 manifest、缓存 bundle、物化 Skill 目录、启动 Agent Runtime | 作为 Skill 权威来源、在服务进程中执行 scripts |
| **Agent Runtime** | 按 Skill 指令调用 Tools、在 sandbox 中执行 scripts | 直接访问数据库、绕过 API Server 调业务数据 |

### 检查清单

- [ ] Skill Release 是否有 API Server / 控制面作为权威来源？
- [ ] 是否区分 Built-in Skill 和动态 Skill Release？
- [ ] 是否记录 releaseId、version、bundleHash、发布人、审核记录？
- [ ] 同名 Skill 是否保证同时最多一个 `published` release？
- [ ] Agent Gateway 是否按 manifest hash / version 同步 Skill bundle？
- [ ] API Server 和 Agent Gateway 是否都不执行 Skill scripts？

---

## 九、认证规范

### 混合认证模式

| 场景 | 认证方式 | 身份 |
|------|---------|------|
| Web 用户操作 | Session Cookie / Bearer Token | 代表具体用户 |
| MCP Client 操作 | 环境变量中的 Access Token | 代表具体用户 |
| 服务间内部调用 | Internal Service Secret | 系统级身份 |
| 定时任务触发 | Service-to-Service Token | 系统级身份 |

### 多租户隔离

- 所有请求必须携带 `tenantKey`（Cookie / Header / Token 内嵌）
- API Server 的每个查询必须带 tenantKey 过滤
- Agent Gateway 透传 tenantKey，不做额外隔离逻辑
- 数据层通过 Row Level Security 或应用层 WHERE 子句隔离

### 检查清单

- [ ] 是否支持用户级和系统级两种认证？
- [ ] 多租户数据隔离是否在 API Server 层强制执行？
- [ ] Agent 操作是否携带正确的用户/租户身份？

---

## 十、部署规范

### 服务拓扑

```
K8s Cluster
├── api-server (Deployment, 2-4 replicas)
│   └── 特征：低延迟、低内存、按 QPS 扩缩
│
├── agent-gateway (Deployment, 2-4 replicas)
│   └── 特征：高延迟(LLM)、高内存(上下文)、按并发 AI 请求数扩缩
│
├── web (Deployment)
├── 业务服务 (Deployments)
├── Workers (Deployments) — 队列消费，触发 Agent 而非直接调 LLM
└── 基础设施 (DB, Redis, etc.)
```

### 资源隔离

| 服务 | CPU Request | Memory Request | 超时配置 |
|------|-------------|---------------|---------|
| API Server | 200m | 256Mi | 30s |
| Agent Gateway | 500m | 512Mi-1Gi | 120s |
| Web | 100m | 128Mi | 30s |

### 检查清单

- [ ] API Server 和 Agent Gateway 是否独立部署？
- [ ] Agent Gateway 是否有独立的扩缩策略？
- [ ] LLM 超时是否不会影响业务 API？
- [ ] 故障隔离：一个网关挂了另一个是否正常？

---

## 十一、项目合规性自查总表

使用此表快速评估一个项目是否符合双网关架构规范：

| # | 检查项 | 状态 |
|---|--------|------|
| 1 | API Server 是所有业务数据的唯一入口 | ⬜ |
| 2 | Agent Gateway 是所有 AI 能力的唯一入口 | ⬜ |
| 3 | 系统中不存在散装 LLM 调用 | ⬜ |
| 4 | 新功能遵循 API-First 流程 | ⬜ |
| 5 | API 端点标注 @AgentTool 实现自动注册 | ⬜ |
| 6 | 实现双模响应（标准 + Agent 增强） | ⬜ |
| 7 | MCP 协议支持（stdio + SSE） | ⬜ |
| 8 | 第三方 Agent 可通过 MCP 无缝接入 | ⬜ |
| 9 | 认证支持用户级 + 系统级混合模式 | ⬜ |
| 10 | 多租户数据严格隔离 | ⬜ |
| 11 | API Server 和 Agent Gateway 独立部署、独立扩缩 | ⬜ |
| 12 | Web 只是展示层，不含直接数据库/LLM 调用 | ⬜ |
| 13 | 复杂业务流程有 Skill.md 编排定义 | ⬜ |
| 14 | Worker 的 AI 逻辑已迁移为 Agent Skill | ⬜ |
| 15 | MCP Tool 与 Skill 的原子动作 / 工作流边界清晰 | ⬜ |
| 16 | 新增 MCP Tool 显式声明 Skill 覆盖策略 | ⬜ |
| 17 | 动态 Skill Release 具备 draft / review / published / superseded / disabled 生命周期 | ⬜ |
| 18 | Skill Release 的权威来源是 API Server 或业务控制面 | ⬜ |
| 19 | Agent Gateway 启动 Agent task 前同步 published Skill manifest | ⬜ |
| 20 | Skill bundle 遵循 SKILL.md + scripts / references / assets 目录结构 | ⬜ |
| 21 | Skill scripts 只在 Agent Runtime workspace / sandbox 中执行 | ⬜ |

---

## 变更记录

| 日期 | 版本 | 变更内容 |
|------|------|---------|
| 2026-05-11 | v1.0 | 初始版本，定义双网关 + API-First + Auto-Registration 规范 |
| 2026-06-03 | v1.1 | 补充动态 Skill 发布、Tool-Skill 覆盖校验、Skill bundle 与 scripts 执行边界规范 |
